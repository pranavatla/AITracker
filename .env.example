# Copy this to .env.local for local development
# Production values are baked in during CI/CD — no secrets needed in GitHub

VITE_COGNITO_DOMAIN=aitracker-auth-361739112666.auth.ap-south-1.amazoncognito.com
VITE_COGNITO_CLIENT_ID=6f5f8iq0pcpugvc5e9rl1se161
VITE_COGNITO_REDIRECT_URI=http://localhost:5173

# Set this after the first SAM deploy (check CloudFormation outputs for the URL)
VITE_API_BASE_URL=https://REPLACE_WITH_API_ID.execute-api.ap-south-1.amazonaws.com/prod
